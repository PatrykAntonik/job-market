apiVersion: apps/v1
kind: Deployment
metadata:
  name: job-market-api
  namespace: jobmarket
  labels:
    app: job-market-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: job-market-api
  template:
    metadata:
      name: job-market-api
      labels:
        app: job-market-api
    spec:
      restartPolicy: Always
      containers:
        - name: job-market-api
          envFrom:
            - configMapRef:
                name: job-market-config
            - secretRef:
                name: api-secret
          image: patryka2000/job-market:1.3
          imagePullPolicy: Always

          ports:
            - containerPort: 8000
              protocol: TCP

          startupProbe:
            httpGet: { path: /health/, port: 8000 }
            failureThreshold: 60
            periodSeconds: 2
            timeoutSeconds: 5
          readinessProbe:
            httpGet: { path: /health/, port: 8000 }
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet: { path: /health/, port: 8000 }
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
