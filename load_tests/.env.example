# Load test configuration (example)
#
# Copy to `load_tests/.env` (untracked) OR point Locust at it:
#   export LOADTEST_ENV_FILE="load_tests/.env"
#
# Base URL of the API under test (set to your environment):
LOCUST_HOST=http://192.168.49.2:31048
#
# Persona weights (canonical order C2/C1/E2/E1). Relative weights; sum-to-100 recommended.
PERSONA_WEIGHTS=80/10/8/2
#
# Seeded account pools (created by `python manage.py seed_loadtest_data`).
# Use relative paths so the same file works for local + Docker (with the mount shown in README).
C2_ACCOUNTS_PATH=load_tests/data/c2_accounts.json
E2_ACCOUNTS_PATH=load_tests/data/e2_accounts.json
#
# Repeatability
LOADTEST_SEED=12345
#
# Think time
WAIT_MIN_SECONDS=1.0
WAIT_MAX_SECONDS=3.0
#
# Candidate behavior
CANDIDATE_APPLY_MIN=3
CANDIDATE_APPLY_MAX=5
CANDIDATE_RESUME_UPLOAD_ENABLED=false
#
# Employer behavior
EMPLOYER_OFFERS_PER_JOURNEY=1
#
# Optional fallback IDs (only needed if reference endpoints are missing/empty)
# DEFAULT_CITY_ID=
# DEFAULT_INDUSTRY_ID=
#
# Optional retries (disabled by default)
HTTP_RETRY_MAX=0
# HTTP_RETRY_BACKOFF_MS=250
# HTTP_RETRY_STATUSES=429,500,502,503,504
#
# Multi-worker sharding for seeded accounts (set when running distributed Locust workers)
LOADTEST_WORKER_INDEX=0
LOADTEST_WORKER_COUNT=1
